// Write a program to search all occurrences of a word in given string.
// Remaining***********************************************
#include <iostream>
#include <string.h>
using namespace std;

int main(){
    char array[100];
    cout <<"Enter string :"<<endl;
    // fgets(array, 100, stdin);
    gets(array); // No need of null character

    char word[20];
    cout <<"Enter word to find all occurrences of it : ";
    gets(word);

    int arrlen = strlen(array);
    int wordlen = strlen(word);

    for()

    
    return 0;
}